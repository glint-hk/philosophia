{
  "name": "philosophia",
  "private": true,
  "version": "0.0.0",
  "type": "module",
  "scripts": {
    "parse-volumes": "node scripts/parse-volumes.js",
    "merge": "node scripts/merge-concepts.js",
    "generate-sitemap": "node scripts/generate-sitemap.js",
    "build-data": "npm run parse-volumes && npm run merge && npm run generate-sitemap",
    "dev": "vite",
    "build": "vite build",
    "lint": "oxlint",
    "preview": "vite preview",
    "predeploy": "npm run generate-sitemap && npm run build",
    "deploy": "gh-pages -d dist"
  },
  "dependencies": {
    "d3": "^7.9.0",
    "file-saver": "^2.0.5",
    "fuse.js": "^7.0.0",
    "jszip": "^3.10.1",
    "react": "^19.2.7",
    "react-dom": "^19.2.7"
  },
  "devDependencies": {
    "@types/react": "^19.2.17",
    "@types/react-dom": "^19.2.3",
    "@vitejs/plugin-react": "^6.0.2",
    "gh-pages": "^6.3.0",
    "oxlint": "^1.69.0",
    "vite": "^8.1.0"
  }
}
